import { CoachPortalLayout } from "@/components/layout/coach-portal-layout";

export default function CoachLayout({ children }: { children: React.ReactNode }) {
  return <CoachPortalLayout>{children}</CoachPortalLayout>;
}
